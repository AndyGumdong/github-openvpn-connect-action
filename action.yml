name: "OpenVPN-Connect-uzaysan"
description: "connect to OpenVPN server"
branding:
  icon: "lock"
  color: "green"
inputs:
  config:
    description: "Content of OpenVPN client config file"
    required: true
runs:
  using: "node16"
  main: "dist/index.js"
  post: "dist/index.js"
